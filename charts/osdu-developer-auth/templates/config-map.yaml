{{- $namespace := .Release.Namespace }}
{{- $clientId := .Values.azure.appId }}
{{- $tenantId := .Values.azure.tenantId }}
{{- $secret := (lookup "v1" "Secret" $namespace "azure-keyvault-credentials") }}
{{- $clientSecret := $secret.data.clientSecret | b64dec }}  # Decode the base64 secret
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: osdu-auth-html
  namespace: {{ $namespace }}
data:
  index.html: |
    <!DOCTYPE html>
    <html lang="en">
    <head>
      <meta charset="utf-8">
      <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
      <meta http-equiv="x-ua-compatible" content="ie=edge">
      <title>OAuth Login</title>
      <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.2/css/all.css">
      <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" rel="stylesheet">
      <style type="text/css">
        textarea {
          margin-bottom: 5px;
        }
      </style>
    </head>
    <body>
      <div id="app">
        <form id="oauth-form" @submit.prevent="exchangeToken" class="text-center border border-light p-5" action="#!">
          <p class="h2 mb-4">Microsoft Identity Platform (v2.0)</p>
          <div class="form-group">
            <div class="form-row">
              <div class="form-group col-md-6">
                <label class="float-left" for="clientId">ClientId</label>
                <input type="text" class="form-control" id="clientId" v-model="clientId" readonly>
              </div>
              <div class="form-group col-md-6">
                <label class="float-left" for="tenantId">TenantId</label>
                <input type="text" class="form-control" id="tenantId" v-model="tenantId" readonly>
              </div>
            </div>
            <div class="form-row">
              <div class="col-md-6 mb-3">
                <label class="float-left" for="redirectUrl">RedirectUrl</label>
                <input type="text" class="form-control" id="redirectUrl" v-model="redirectUrl" readonly>
              </div>
              <div class="col-md-3 mb-3">
                <label class="float-left" for="responseType">ResponseType</label>
                <input type="text" class="form-control" id="responseType" v-model="responseType" readonly>
              </div>
              <div class="col-md-3 mb-3">
                <label class="float-left" for="responseMode">ResponseMode</label>
                <input type="text" class="form-control" id="responseMode" v-model="responseMode" readonly>
              </div>
            </div>
            <div class="form-row">
              <div class="col-md-12 mb-3">
                <label class="float-left" for="scope">Scope</label>
                <input type="text" class="form-control" id="scope" v-model="scope" readonly>
              </div>
            </div>
            <div class="form-row">
              <div class="col-md-12 mb-1">
                <a :href="authorizeUrl" class="btn btn-primary float-right">Authorize</a>
              </div>
            </div>
          </div>
          <hr />
          <div class="form-group shadow-textarea">
            <div class="form-row">
              <div class="col-md-4 mb-4">
                <label class="float-left" for="authorizationCode">Authorization Code</label>
                <textarea id="authorizationCode" v-model="authorizationCode" class="form-control z-depth-1" rows="5" readonly></textarea>
              </div>
              <div class="col-md-4 mb-4">
                <label class="float-left" for="accessToken">Access Token</label>
                <textarea id="accessToken" v-model="accessToken" class="form-control z-depth-1" rows="5" readonly></textarea>
                <a @click="decodeAccess()" class="btn btn-info btn-sm float-right" v-if="accessToken">Decode</a>
              </div>
              <div class="col-md-4 mb-4">
                <label class="float-left" for="refreshToken">Refresh Token</label>
                <textarea id="refreshToken" v-model="refreshToken" class="form-control z-depth-1" rows="5" readonly></textarea>
              </div>
            </div>
            <div class="form-row">
              <div class="col-md-12 mb-1">
                <button @click="exchangeToken" class="btn btn-success float-right" v-if="authorizationCode">Get Tokens</button>
              </div>
            </div>
          </div>
        </form>
      </div>
      <!-- SCRIPTS -->
      <script src="https://code.jquery.com/jquery-3.2.1.slim.min.js"></script>
      <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js"></script>
      <script src="https://cdn.jsdelivr.net/npm/vue/dist/vue.js"></script>
      <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script> <!-- Axios script -->
      <script>
        var app = new Vue({
          el: '#app',
          data: {
            tenantId: '{{ $tenantId }}',
            clientId: '{{ $clientId }}',
            clientSecret: '{{ $clientSecret }}', // Decoded client secret
            redirectUrl: window.location.href,
            responseType: 'code',
            responseMode: 'fragment',
            scope: '{{ $clientId }}/.default openid profile offline_access',
            authorizationCode: null,
            accessToken: null,
            refreshToken: null,
          },
          computed: {
            authorizeUrl: function () {
              return "https://login.microsoftonline.com/" + this.tenantId +
                "/oauth2/v2.0/authorize?" +
                "client_id=" + this.clientId +
                "&response_type=" + this.responseType +
                "&redirect_uri=" + this.redirectUrl +
                "&response_mode=" + this.responseMode +
                "&scope=" + this.scope +
                "&state=12345" +
                "&nonce=dummy123";
            }
          },
          methods: {
            exchangeToken: function() {
              if (this.authorizationCode) {
                axios.post('https://login.microsoftonline.com/' + this.tenantId + '/oauth2/v2.0/token', new URLSearchParams({
                  client_id: this.clientId,
                  client_secret: this.clientSecret,
                  grant_type: 'authorization_code',
                  code: this.authorizationCode,
                  redirect_uri: this.redirectUrl
                }))
                .then(response => {
                  this.accessToken = response.data.access_token;
                  this.refreshToken = response.data.refresh_token;
                  this.authorizationCode = null;  // Clear the authorization code after exchange
                })
                .catch(error => {
                  console.error(error);
                });
              }
            },
            decodeAccess: function () {
              var decodeUrl = "https://jwt.ms/#id_token=" + this.accessToken;
              window.open(decodeUrl, "_blank");
            }
          },
          beforeMount: function () {
            if (window.location.hash) {
              var params = window.location.hash.substr(1).split('&').reduce(function (result, item) {
                var parts = item.split('=');
                result[parts[0]] = parts[1];
                return result;
              }, {});
              this.authorizationCode = params['code'];
            }
          }
        });
      </script>
    </body>
    </html>
